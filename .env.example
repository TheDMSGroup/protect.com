# Google Sheets API Key (optional - falls back to default in nuxt.config.ts)
# Get your API key from: https://console.cloud.google.com/apis/credentials
GOOGLE_SHEETS_API_KEY=your-google-api-key-here

# Cache clear token (required - change this to a random secure string)
# Generate with: openssl rand -base64 32
# Used to clear cache via: /api/sheets/clear-cache?token=YOUR_TOKEN
CACHE_CLEAR_TOKEN=your-secure-random-token-here
